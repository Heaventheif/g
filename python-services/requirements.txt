# ─── متطلبات البنية التحتية (ثابتة) ───────────────────────────
# ─── متطلبات البنية التحتية — لا تُعدَّل ────────────────────────────
# هذا الملف للـ framework فقط (FastAPI, uvicorn, pydantic).
# متطلبات كل plugin تُعلَن في Plugin.requirements داخل ملف الـ plugin.
fastapi
uvicorn[standard]
pydantic

# ─── pip extras من plugins ─────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

# ─── حزم plugins (مجموعة تلقائياً) ────────────────────────────
requests
pillow
opencv-python
numpy
paddlepaddle
paddleocr
transformers
sentencepiece
protobuf
torch
